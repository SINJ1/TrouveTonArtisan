import { SortbynotePipe } from './sortbynote.pipe';

describe('SortbynotePipe', () => {
  it('create an instance', () => {
    const pipe = new SortbynotePipe();
    expect(pipe).toBeTruthy();
  });
});
